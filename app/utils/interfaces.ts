export interface Stock {
  country: string;
  currency: string;
  exchange: string;
  mic_code: string;
  name: string;
  symbol: string;
  type: string;
}

export interface SelectOption {
  label: string;
  value: string;
}